# --- General Settings ---
TZ=Asia/Bangkok              # กำหนด Timezone ของคุณ (เช่น Asia/Bangkok)

# --- n8n Settings ---
N8N_PORT=5678                # พอร์ตที่ n8n จะรันภายในคอนเทนเนอร์ (ไม่ต้องเปลี่ยน)
N8N_PROTOCOL=http            # ใช้ HTTP เนื่องจากไม่มี SSL จาก Let's Encrypt
# N8N_HOST จะถูกละไว้ หรือตั้งเป็น IP Address ของเครื่องคุณ หากต้องการให้เครื่องอื่นในเครือข่ายเข้าถึงได้

# --- PostgreSQL Database Settings ---
POSTGRES_DB=n8n
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres # **เปลี่ยนเป็นรหัสผ่านที่รัดกุม!**